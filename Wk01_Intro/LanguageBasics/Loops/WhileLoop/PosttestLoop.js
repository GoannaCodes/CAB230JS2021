/* Post-tested Loop using do-while loop */
//counter declaration/initialisation
let count = 0; 

//execute the following code once
do { 
  //increment counter value
  count++; 
  //display current value of count
  console.log(count); 
} while (count < 100); //if condition is still true, iterate loop again
