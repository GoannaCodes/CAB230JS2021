/* Pre-tested loop using while loop */
let count = 1; //counter declaration
while (count <= 100) { //condition to execute while loop i.e while count is less than or equal to 100
  console.log(count); //display current value of count
  count++; //increment count, so that a new number is tested
}
